Use case: ui-mockup
Asset type: high-fidelity mobile motion storyboard for DND/D20 custom dice pool
Primary request: Create an implementation-ready five-state Pocketools DND/D20 custom dice-pool storyboard showing press, generate, reveal, complete, and reduced-motion behavior. All dice are generated once, kept or discarded by the frozen rule, and persisted before reveal. This is a precise product UI specification, not a game HUD or concept illustration.

Canvas and framing: One exact 3840 x 2160 landscape canvas with an opaque deep-neutral backdrop. Place five complete equal portrait mobile frames in one horizontal sequence, each preserving the 360 x 800 viewport ratio, full border, consistent scale, and readable 4K typography. Use a thin timeline baseline and one concise timing chip per frame; no cropped screens or branded devices.

Shared visual language: Pocketools is a restrained, refined modern utility toolbox. Use a deep neutral shell with #111416 background, #181C1F work surfaces, #22282C muted surfaces, #101619 inset data areas, #293036 raised surfaces, #F4F6F7 primary text, #BEC6CC secondary text, and #46515A borders. D20 uses #75D2E3 and #8CB7F0 only for selected controls, focus, kept dice, and the primary action. Build depth through whitespace, precise typography, matte materials, subtle 1 px edge highlights, and restrained shadows. Use a system sans-serif, tabular numerals, an 8-point grid, 16 px standard card radius, 12 px control radius, 48 px minimum touch targets, and a visible 2 px focus ring. Do not imitate a video-game dashboard, fantasy tavern, branded tabletop system, or casino table.

Frozen example used in every frame: "自定义 · 4D20 · 保留最高 3 · +5 · DC 35". Original generation order and values are #1 = 8 kept, #2 = 16 kept, #3 = 4 discarded, #4 = 12 kept. Total is 16 + 12 + 8 + 5 = 41, reaching DC 35. Never sort the displayed original sequence and never add proficiency bonus.

Navigation invariant used in every frame: Show the four-item mobile bottom navigation in the exact order "首页", "工具", "历史", "设置" with "工具" selected and "首页" unselected. During locked generation the navigation may be semantically disabled but its selected route remains "工具". Never replace the bottom navigation with a hamburger menu, top drawer button, or device-level navigation.

Five frames, left to right:
1. "按压 · 90ms": The primary button reads exactly "掷骰" and is pressed by translateY 1 px and scale 0.985, with zero blur and no particles. The full frozen summary remains visible. Do not show result values yet.
2. "生成 · 360ms": Inputs are locked, action reads "正在生成结果", four generic geometric dice placeholders remain unrevealed, scale settles from 0.985 to 1, blur never exceeds 0.6 px, and no particles appear. Do not show any die value, kept/discarded label, total, or DC outcome yet.
3. "揭示 · 640ms": Status reads "结果已生成，正在揭示". Reveal all four dice in original order with at most 24 ms stagger, at most 6 degrees short rocking, 12 px to 0 travel, scale 0.96 to 1, blur 8 to 0 px, and at most 12 tiny cyan-blue points or short lines. As values become visible, attach explicit "保留" or "舍弃" labels; the discarded die remains legible. A product action "跳过动画" may appear only in this reveal frame as one secondary text control; it must not appear in press, generation, complete, or reduced-motion frames.
4. "完成 · 240ms": Show all four numbered dice in original order, "总值 41", formula "16 + 12 + 8 + 5 = 41", and "达到 DC 35". Use 4 px to 0 settle, scale 1.02 to 1, and at most 8 restrained non-celebratory particles. The primary action reads exactly "再来一次" and a compact secondary control reads "查看过程". Do not show "掷骰" in this completed state.
5. "减少动态 · 80ms": Show the exact same completed dice, labels, formula, DC status, primary action "再来一次", and compact "查看过程" control through opacity only. No rocking, translation, scale, blur, stagger, or particles. A small chip reads "减少动态". Do not show "掷骰" in this state.

Numeric and state consistency gate: Treat the example as one immutable record in every frame: 4D20, keepHighest 3, modifier +5, DC 35; original order #1 8 kept, #2 16 kept, #3 4 discarded, #4 12 kept; total 41. The displayed sequence must remain exactly 8 / 16 / 4 / 12, #3 is the only discarded die, and every summary, label, formula, total, and DC statement must agree. Do not add a proficiency bonus, natural-20 rule, fifth die, different modifier, different DC, or conflicting total.

Motion semantics: Use timing annotations 90ms, 360ms, 640ms, 240ms, 80ms. Press easing is cubic-bezier(0.2,0,0,1); generation is cubic-bezier(0.4,0,0.2,1); reveal is cubic-bezier(0.16,1,0.3,1); complete is cubic-bezier(0.2,0,0,1); reduced motion is linear. Communicate skip, interruption, and resume as a concise non-interactive timeline annotation outside the app frames, converging on the same persisted result without rerolling. Do not turn storyboard semantics into persistent product controls.

Typography and hierarchy: Render only the quoted Chinese, exact numeric values, dice sequence numbers, and timings. Use text, outline, and luminance together for kept and discarded states. Keep all text crisp and stationary.

Constraints: High fidelity, modern motion expression, refined typesetting, controlled material depth, practical Flutter/Web components, clear hierarchy, accessible labels, opaque background. Build entirely from this revised written specification. Do not use any prior rejected image as an edit input, visual reference, layout source, or asset source.

Avoid and prohibit: no watermark, no brand, no trademark, no real or realistic currency, no traditional or copyrighted tarot card artwork, no third-party rulebook art, no casino or gambling imagery, no concept art, no low-fidelity wireframe, no cheap gradient, no exaggerated glassmorphism, no transparent background, no game HUD, no hamburger menu, no missing bottom navigation, no persistent debug row, no controls labeled "跳过揭示", "中断", or "继续", no reveal control outside the reveal frame, no completed-state "掷骰" button, no critical-success celebration, no natural-20 rule, no proficiency bonus, no extra dice, no reordered dice, no confetti, and no third-party symbol.
