Use case: ui-mockup
Asset type: independent high-fidelity responsive desktop product page source
Primary request: Create one polished, implementation-ready Pocketools v0.1.0 desktop DND/D20 custom dice-pool configuration page for the round-3 web set. This is one page source, not a four-page board, not a landing page, and not concept art.

Source raster and safe frame: Request one opaque 1672 × 941 source PNG as the 16:9 presentation raster. Treat that as a task request only; do not claim that the platform natively supports a custom 1506 × 941 or 1600 × 1000 output, and do not invent model metadata. All page content must stay inside the exact central 1506 × 941 16:10 safe frame at source coordinates x=83, y=0, width=1506, height=941. The left x=0..82 and right x=1589..1671 gutters are pure presentation background only: no interface, text, border, shadow, caption, crop mark, or device chrome. After generation, deterministically crop (83,0,1506,941), then resize that crop to exactly 1600 × 1000 for assembly; this is local post-processing, not a native platform canvas claim.

Desktop layout baseline: Build one true 1440 × 900 desktop page inside the safe frame, with a full readable viewport, a 240 px left navigation rail, 32 px content gutters, and a maximum 1120 px inner content region. Do not create a second nested frame or a different-aspect presentation card. Keep the page border and all content inside the safe frame so the later crop has no clipping.

Shared visual language: Pocketools is a restrained, refined modern utility toolbox. Use light surfaces #F7F7F5, #FFFFFF, #F0F1EF, and #E8EBE8, primary text #1B1D1F, secondary text #555B61, border #D6DADD, and D20 primary #006D82. Use system sans-serif, tabular numerals, an 8-point grid, 16 px card radius, 12 px control radius, 48 px minimum targets, and visible 2 px focus rings. Use whitespace, typography, dividers, matte surfaces, and restrained shadows; do not use a game HUD, cheap gradients, glassmorphism, or decorative fantasy imagery.

Desktop shell: Show the navigation entries in the exact order "首页", "工具", "历史", "设置". Select only "工具" with background, left indicator, and weight; the other entries are visibly unselected. Do not show bottom navigation, a hamburger menu, account, avatar, cloud sync, analytics, marketing navigation, browser chrome, or a third-party logo.

D20 configuration content: Use a two-column desktop layout with a 520 px configuration column, a 24 px gap, and a 520 px structured summary column. Show the mode selector in the exact order "普通", "优势", "劣势", "自定义", with "自定义" selected. Show diceCount 4 with visible range 1–20; D20 selected among D4, D6, D8, D10, D12, D20, D100; a custom-sides field with visible range 2–1000; aggregation choices "全部求和", "保留最高", "保留最低" with "保留最高" selected; K 3 with visible range 1–4; modifier +5; DC enabled and set to 35. Each count, side, and K field has practical minus, input, and plus controls. The right inset summary reads exactly "自定义 · 4D20 · 保留最高 3 · +5 · DC 35" and explains the formula shape without preview dice or result values. One primary button reads exactly "掷骰". Show one realistic keyboard focus ring on a field without changing the data.

Typography and fidelity gate: Render the quoted Chinese, exact ranges, and exact D20 values crisply. The page is a configuration state, so do not show generated dice, kept or discarded labels, total 41, DC outcome, proficiency bonus, natural-20 rules, extra dice, or any preview result.

Constraints and prohibitions: Opaque background; no watermark, brand, trademark, real or realistic currency, denomination, traditional or copyrighted tarot artwork, third-party tabletop logo, casino imagery, concept art, low-fidelity wireframe, tiny illegible text, impossible compact controls, transparent gutter, UI in either gutter, or extra rule fields. Do not use any prior rejected image as an edit input, visual reference, layout source, or asset source.
