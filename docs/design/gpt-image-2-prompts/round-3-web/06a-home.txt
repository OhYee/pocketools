Use case: ui-mockup
Asset type: independent high-fidelity responsive desktop product page source
Primary request: Create one polished, implementation-ready Pocketools v0.1.0 desktop home page for the round-3 web set. This is one page source, not a four-page board, not a landing page, and not concept art.

Source raster and safe frame: Request one opaque 1672 × 941 source PNG as the 16:9 presentation raster. Treat that as a task request only; do not claim that the platform natively supports a custom 1506 × 941 or 1600 × 1000 output, and do not invent model metadata. All page content must stay inside the exact central 1506 × 941 16:10 safe frame at source coordinates x=83, y=0, width=1506, height=941. The left x=0..82 and right x=1589..1671 gutters are pure presentation background only: no interface, text, border, shadow, caption, crop mark, or device chrome. After generation, deterministically crop (83,0,1506,941), then resize that crop to exactly 1600 × 1000 for assembly; this is local post-processing, not a native platform canvas claim.

Desktop layout baseline: Build one true 1440 × 900 desktop page inside the safe frame, with a full readable viewport, a 240 px left navigation rail, 32 px content gutters, and a maximum 1120 px inner content region. Do not create a second nested frame or a different-aspect presentation card. Keep the page border and all content inside the safe frame so the later crop has no clipping.

Shared visual language: Pocketools is a restrained, refined modern utility toolbox. Use light surfaces #F7F7F5, #FFFFFF, #F0F1EF, and #E8EBE8, primary text #1B1D1F, secondary text #555B61, border #D6DADD, and neutral brand #245E73. Use system sans-serif, tabular numerals, an 8-point grid, 16 px card radius, 12 px control radius, 48 px minimum targets, and visible 2 px focus rings. Use whitespace, typography, dividers, matte surfaces, and restrained shadows; do not use a template grid of identical floating cards, cheap gradients, glassmorphism, or decorative mysticism.

Desktop shell: Show the navigation entries in the exact order "首页", "工具", "历史", "设置". Select only "首页" with background, left indicator, and weight; the other entries are visibly unselected. Do not show bottom navigation, a hamburger menu, account, avatar, cloud sync, analytics, marketing navigation, browser chrome, or a third-party logo.

Home content: Show the exact heading "今天想用哪个工具？". Show four distinct implementable tool entries labeled exactly "塔罗", "六爻起卦", "D20 检定", and "抛硬币". Each entry uses its own original abstract line symbol and spatial composition; do not make four recolored copies of one icon. Add a compact offline banner with the exact text "当前离线，核心工具仍可使用。" without blocking any tool. Add a concise "最近结果" region using only synthetic, non-private fixed summaries: "D20 检定 · 自定义 · 总值 41 · 达到 DC 35" and "抛硬币 · 批量 10 · 正面 5 · 反面 5". Do not show private questions, notes, dates, names, account information, or invented analytics.

Typography and fidelity gate: Render the quoted Chinese exactly, keep the four tool names and recent summaries readable at desktop scale, and do not add fields or prose. The selected route, offline state, tool order, and fixed D20 and coin summaries must be unambiguous.

Constraints and prohibitions: Opaque background; no watermark, brand, trademark, real or realistic currency, denomination, traditional or copyrighted tarot artwork, third-party tabletop logo, casino imagery, concept art, low-fidelity wireframe, tiny illegible text, fake research, account avatar, analytics, transparent gutter, UI in either gutter, or extra navigation. Do not use any prior rejected image as an edit input, visual reference, layout source, or asset source.
