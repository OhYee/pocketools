Use case: ui-mockup
Asset type: high-fidelity responsive desktop product UI board
Primary request: Create a polished, implementation-ready Pocketools v0.1.0 desktop web board showing a true 1440 x 900 home, DND/D20 custom dice-pool configuration, complete DND/D20 result, and local history with share-preview hierarchy. This is a shippable responsive product interface specification, not a landing page or concept art.

Canvas and exact composition: One exact 3840 x 2160 landscape canvas with an opaque neutral presentation background. Arrange four complete desktop frames in a strict 2 x 2 grid. Each child frame itself, including its full outer border, is exactly 1600 x 1000 pixels and therefore exactly 16:10; this requirement applies independently to every child frame, not merely to the overall canvas or grid. Place frame 1 at x=288, y=48; frame 2 at x=1952, y=48; frame 3 at x=288, y=1112; frame 4 at x=1952, y=1112. These coordinates create 288 px left and right margins, 48 px top and bottom margins, and 64 px gaps. Build each child interface in a true 1440 x 900 coordinate system and apply one uniform 10/9 scale to reach 1600 x 1000. Show every child border in full with no crop, perspective, tilt, vertical compression, horizontal stretch, or device chrome.

Geometry compliance gate: Before adding UI details, establish four equal 1600 x 1000 rectangles at the exact coordinates above. Do not place a 16:9, 1.85:1, ultrawide, tablet, or freeform panel inside any child frame. Do not nest a browser window, presentation card, or second frame with a different aspect ratio inside a child frame. Do not shorten the child frames to create caption space. Captions, if needed, sit in the 64 px inter-frame gaps without changing any frame geometry. All four child frames must remain visually uncompressed and identically scaled.

Shared visual language: Pocketools is a restrained, refined modern utility toolbox. Use a neutral shell with light surfaces #F7F7F5, #FFFFFF, #F0F1EF, #E8EBE8, text #1B1D1F and #555B61, border #D6DADD; dark surfaces #111416, #181C1F, #22282C, #101619, raised #293036, text #F4F6F7 and #BEC6CC, border #46515A. D20 accents are #006D82 in light theme and #75D2E3 in dark theme. Build depth through whitespace, precise typography, matte planes, subtle 1 px borders and highlights, and restrained shadows. Use a system sans-serif, tabular numerals, an 8-point grid, 16 px standard card radius, 12 px control radius, 48 px minimum targets, and visible 2 px focus rings. Group with headings, whitespace, dividers, and inset data regions; avoid a template grid of identical floating cards.

Desktop shell in every frame: A true 240 px left navigation rail inside the 1440 x 900 viewport, 32 px content gutters, maximum 1120 px inner content, and navigation "首页", "工具", "历史", "设置" in that order. Show a clear selected state using background, left indicator, and weight. No bottom navigation, hamburger menu, account, avatar, cloud sync, analytics dashboard, marketing navigation, or browser brand.

Frame 1, light-theme home: Selected navigation "首页". Show heading "今天想用哪个工具？", four distinct implementable tool entries "塔罗", "六爻起卦", "D20 检定", "抛硬币", and a concise recent-results region using synthetic non-private summaries only. The entries use original abstract line symbols and distinct spatial compositions, not identical recolored templates. Include a compact offline banner "当前离线，核心工具仍可使用。" without blocking any tool.

Frame 2, light-theme DND/D20 configuration: Selected navigation "工具". Use a two-column desktop layout: 520 px configuration and 520 px structured summary separated by 24 px. Show "普通", "优势", "劣势", "自定义" with custom selected; diceCount 4 with range 1–20; D20 selected from D4, D6, D8, D10, D12, D20, D100 plus custom sides range 2–1000; "保留最高" selected; K 3 with range 1–4; modifier +5; DC 35. Each count, side, and K field has practical minus/input/plus controls. The right inset summary reads "自定义 · 4D20 · 保留最高 3 · +5 · DC 35" and explains the formula shape without preview dice. One primary button reads "掷骰". Show one realistic keyboard focus state.

Frame 3, dark-theme complete DND/D20 result: Selected navigation "工具". Use a 7:5 result/process split. Keep original dice order exactly: #1 = 8 "保留", #2 = 16 "保留", #3 = 4 "舍弃", #4 = 12 "保留". Show all dice, "总值 41", formula "16 + 12 + 8 + 5 = 41", "达到 DC 35", and an expanded "查看过程" area with aggregation, K, modifier, DC, rule version, and stable kept/discarded semantics. Include actions "再来一次", "复制文本", "分享". Do not add proficiency bonus, natural-20 rules, or any extra dice.

Frame 4, dark-theme history plus share hierarchy: Selected navigation "历史". Show a true 280 px filter column, readable local history list, and selected detail region. List rows reveal only tool, mode, result summary, and relative time, never private questions or notes. Over the selected result, open an opaque raised share-preview modal or side sheet with a dim scrim, clear focus containment, title "分享前预览", default-hidden options "问题", "备注", "精确时间", and a final preview containing only tool, rule summary, result, and rule version. Show "复制文本" as the safe primary fallback. The modal must visibly sit above history while preserving the desktop layer relationship.

DND numeric consistency gate: Frames 2 and 3 share one immutable record: custom mode, diceCount 4, diceSides 20, keepHighest K 3, modifier +5, DC 35, original values #1 8 / #2 16 / #3 4 / #4 12, kept #1 / #2 / #4, discarded #3, total 41. Every field, summary, result, formula, history row, and share preview that repeats this record must agree exactly. Never add proficiency bonus, a separate skill bonus, natural-20 semantics, another die, a different DC, or a conflicting total.

Typography and hierarchy: Render only the specified minimal Chinese, exact numeric values, short version text, and necessary field labels. Render quoted strings verbatim and do not invent dates, private data, proficiency fields, research notes, or additional navigation. Check every repeated word and number for exact agreement before final rendering. Keep each child frame readable as a real 1440 x 900 desktop viewport, with desktop-scale sidebar, content density, and vertical hierarchy intact.

Motion expression: Although static, show precise modern microinteraction cues: one pressed button, one focused field, one locked generated status, one settled result, and one reduced-motion chip across the board. Do not use motion blur on text or imply that animation generates random values.

Constraints: High fidelity, clear desktop hierarchy, refined typesetting, controlled material depth, realistic responsive components, accessible contrast, opaque background. Build entirely from this revised written specification. Do not use any prior rejected image as an edit input, visual reference, layout source, or asset source.

Avoid and prohibit: no watermark, no brand, no trademark, no real or realistic currency, no denomination, no traditional or copyrighted tarot card artwork, no third-party tabletop logo or rulebook art, no casino, gambling table, poker chip, concept art, low-fidelity wireframe, cheap gradient, exaggerated glassmorphism, transparent background, landing-page hero, tiny illegible text, impossible overlapping panes, cropped frame, 16:9 child frame, 1.85:1 child frame, ultrawide child frame, non-uniform scaling, nested mismatched frame, tablet-like narrow columns, account avatar, analytics, fake research, celebratory confetti, extra rule field, or third-party symbol.
