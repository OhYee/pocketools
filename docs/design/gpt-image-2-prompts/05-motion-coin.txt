Use case: ui-mockup
Asset type: high-fidelity mobile motion storyboard for abstract coin tossing
Primary request: Create an implementation-ready five-state Pocketools batch coin-toss storyboard showing press, generate, reveal, complete, and reduced-motion behavior. The full binary sequence is generated once and persisted before reveal. This is a modern neutral random-tool UI, not currency, gambling, or concept art.

Canvas and framing: One exact 3840 x 2160 landscape canvas with an opaque warm-neutral backdrop. Place five complete equal portrait mobile frames in one horizontal sequence, each preserving the 360 x 800 viewport ratio, full border, consistent scale, and readable 4K typography. Use a thin timeline baseline and one concise timing chip per frame; no cropped screens or branded phone hardware.

Shared visual language: Pocketools is a restrained, refined modern utility toolbox. Use a neutral light shell with #F7F7F5 canvas, #FFFFFF work surfaces, #F0F1EF muted surfaces, #E8EBE8 inset data areas, #1B1D1F primary text, #555B61 secondary text, and #D6DADD borders. Coin uses amber #A65B00 and warm graphite #6C563A on #FFF3DF only for selected controls and abstract two-sided markers. Build depth through whitespace, precise typography, matte materials, subtle 1 px borders, and restrained shadows. Use a system sans-serif, tabular numerals, an 8-point grid, 16 px standard card radius, 12 px control radius, 48 px minimum touch targets, and a visible 2 px focus ring. Do not use gold bullion, minted metal, poker chips, casino tables, prize counters, or glossy skeuomorphism.

Frozen example and single source of truth: Batch count 10 with the exact ordered sequence "正、正、反、正、正、反、反、正、反、反". Indexed values are #1 正, #2 正, #3 反, #4 正, #5 正, #6 反, #7 反, #8 正, #9 反, #10 反. The only correct summary is "正面 5", "反面 5", "正面 50%", and "反面 50%". Use flat abstract dual-state markers labeled only "正" and "反", with no denomination, portrait, seal, emblem, national symbol, milled edge, coin engraving, monetary texture, or physical coin thickness.

Navigation invariant: Every one of the five complete mobile app frames shows the same four-item bottom navigation in the exact order "首页", "工具", "历史", "设置". This is the coin tool route, so "工具" is selected in every frame and the other three items are visibly unselected. During generation the navigation may be semantically disabled, but it remains visible and never becomes a hamburger menu, top drawer, or device-level navigation.

Five frames, left to right:
1. "按压 · 90ms": The mode selector shows "单次" and "批量" with "批量" selected. Show batch shortcuts "3", "5", and "10", count set to 10, and the exact range hint "1～100". The "抛 10 次" button is pressed by translateY 1 px and scale 0.985, with zero blur and no particles. Batch configuration remains visible. Never show 1000 as a batch limit. The complete app frame includes "首页", "工具", "历史", "设置" in that order with only "工具" selected.
2. "生成 · 360ms": Configuration is locked, action reads "正在生成结果", and the exact range hint remains "1～100". Show ten separate flat unresolved placeholders in one non-overlapping lane or grid. They have uniform matte fill, no face labels, no depth, no thickness, no rim, no specular metal, and no stack. Scale settles from 0.985 to 1, blur never exceeds 0.6 px, and no particles appear. The complete app frame still includes the four-item bottom navigation in the exact order with only "工具" selected.
3. "揭示 · 640ms": Status reads "结果已生成，正在揭示". Reveal a compact ordered stream using the exact indexed sequence "正、正、反、正、正、反、反、正、反、反". Each flat marker enters from 12 px to 0, scale 0.96 to 1, blur 8 to 0 px. A single lead marker may rotate no more than 360 degrees; do not rotate all ten. Use at most 12 tiny amber points or short lines, never a coin shower. Do not show a summary that disagrees with the revealed sequence. The complete app frame includes the exact four-item bottom navigation with only "工具" selected.
4. "完成 · 240ms": Show "共抛 10 次", summary "正面 5", "反面 5", "正面 50%", "反面 50%", and the full exact ordered sequence "正、正、反、正、正、反、反、正、反、反". Use 4 px to 0 settle, scale 1.02 to 1, and at most 8 restrained non-celebratory particles. Include "再来一次" only now. The complete app frame includes "首页", "工具", "历史", "设置" in that order with only "工具" selected.
5. "减少动态 · 80ms": Show the exact same completed 5-to-5 summary, 50%-to-50% proportions, and ordered sequence through opacity only, with no rotation, translation, scale, blur, or particles. A small chip reads "减少动态". The complete app frame includes the same four-item bottom navigation with only "工具" selected.

Count and asset consistency gate: The custom batch range is exactly 1～100 and never 1～1000. The ten-item sequence has exactly five "正" and five "反"; every count, percentage, index, marker, reveal frame, completed frame, and reduced-motion frame must agree. Recount the ten visible markers before final rendering. All markers are abstract UI glyphs, not physical objects: use flat vector-like shape, uniform matte color, no metallic reflection, no embossed rim, no thickness, no pile, no stack, no denomination, and no currency semantics.

Motion semantics: Use timing annotations 90ms, 360ms, 640ms, 240ms, 80ms. Press easing is cubic-bezier(0.2,0,0,1); generation is cubic-bezier(0.4,0,0.2,1); reveal is cubic-bezier(0.16,1,0.3,1); complete is cubic-bezier(0.2,0,0,1); reduced motion is linear. Visually communicate that skip, interruption, or resume shows the same persisted ten-item sequence and never tosses again.

Typography and hierarchy: Render only the quoted Chinese, ten abstract face markers, numeric proportions, and timings. Keep counts, sequence, and next action in clear product hierarchy. The five states must be consecutive versions of one interface.

Constraints: High fidelity, modern motion expression, refined typesetting, controlled material depth, practical Flutter/Web components, clear hierarchy, accessible labels, opaque background. Build entirely from this revised written specification. Do not use any prior rejected image as an edit input, visual reference, layout source, or asset source.

Avoid and prohibit: no watermark, no brand, no trademark, no real or realistic currency, no denomination, no traditional or copyrighted tarot card artwork, no casino, gambling table, poker chip, prize, slot machine, concept art, low-fidelity wireframe, cheap gradient, exaggerated glassmorphism, transparent background, physical coin, metal coin, gold coin, silver coin, metallic disc stack, piled markers, minted edge, coin engraving, currency texture, coin rain, win celebration, 1～1000 range, 6-to-4 summary, 60%-to-40% summary, extra text, fake probability claim, confetti, or third-party symbol.
