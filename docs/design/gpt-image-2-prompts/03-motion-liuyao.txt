Use case: ui-mockup
Asset type: high-fidelity mobile motion storyboard for Liuyao
Primary request: Create an implementation-ready five-state Pocketools automatic three-disc interaction storyboard for the tool titled exactly "六爻起卦", showing press, generate, reveal, complete, and reduced-motion behavior. Each line result is generated and stored before its reveal. This is a modern utility UI specification, not historical or mystical concept art. Never render any alternate title, homophone, or typo.

Canvas and framing: One exact 3840 x 2160 landscape canvas with an opaque warm-neutral backdrop. Place five complete equal portrait mobile frames in one horizontal sequence, each preserving the 360 x 800 viewport ratio, full border, consistent scale, and readable 4K typography. Use a thin timeline baseline and one concise timing chip per frame; no cropped screens or branded device chrome.

Shared visual language: Pocketools is a restrained, refined modern utility toolbox. Use a neutral light shell with #F7F7F5 canvas, #FFFFFF work surfaces, #F0F1EF muted surfaces, #E8EBE8 inset data areas, #1B1D1F primary text, #555B61 secondary text, and #D6DADD borders. Liuyao uses ink green #285B46 and warm accent #7A5C2E on #F5F1E7 only in focused graphics and actions. Build depth through whitespace, precise typography, matte materials, subtle 1 px borders, and restrained shadows. Use a system sans-serif, an 8-point grid, 16 px standard card radius, 12 px control radius, 48 px minimum touch targets, and a visible 2 px focus ring. Do not imitate parchment, scrolls, ink paintings, temples, talismans, or antique objects.

Title and navigation invariant: Every complete app frame has the exact page title "六爻起卦" and shows the same four-item mobile bottom navigation in the exact order "首页", "工具", "历史", "设置". This is the tool route, so "工具" is selected in every frame and the other three items are visibly unselected. During generation the navigation may be semantically disabled, but it remains visible and never becomes a hamburger menu, top drawer, or device-level navigation.

Five frames, left to right:
1. "按压 · 90ms": The mode selector shows "自动投币" and "手工录入" with "自动投币" selected. The "投下一爻" button is pressed by translateY 1 px and scale 0.985, with zero blur and no particles. The exact title is "六爻起卦" and progress reads "第 3／6 爻". Show exactly two completed lines in bottom-to-top generation order: the bottom first line is a solid unbroken yang line labeled "初爻 · 阳 · 静爻", and the second line directly above it is a solid unbroken yang line labeled "二爻 · 阳 · 静爻". The third result slot is a completely neutral empty placeholder: use the same flat neutral fill, shape, size, border, and opacity as every empty result slot in the app; it must not use a solid or broken line, segment, symbol, face label, 正／反 color, orientation, brightness, or contrast that could predict the result. The complete app frame includes "首页", "工具", "历史", "设置" in that order with only "工具" selected.
2. "生成 · 360ms": The exact title remains "六爻起卦". The action is locked and reads "正在生成结果". Progress remains "第 3／6 爻". Keep the same two solid completed yang lines and labels unchanged. Keep the third result slot exactly the same neutral empty placeholder as frame 1, with the same color and shape and with no line style, symbol, 正／反 distinction, orientation, brightness, or contrast prediction. Three unresolved flat abstract placeholders sit separately in a matte inset stage, all identical and unresolved, scale settles from 0.985 to 1, blur never exceeds 0.6 px, and no particles appear. Do not reveal any face marker or third-line result. The complete app frame includes the four-item bottom navigation and only "工具" selected.
3. "揭示 · 640ms": The exact title is "六爻起卦" and status reads "结果已生成，正在揭示". Reveal the three abstract two-sided markers in the exact order "正、反、正", corresponding exactly to "3 + 2 + 3 = 8". Only after those markers are visible, move one new broken yin line upward no more than 8 px into the third position above the second line. Label it exactly "三爻 · 阴 · 静爻" and show "和值 8 · 少阴 · 静爻". The broken line, "阴", "少阴", and "静爻" must all agree, and the stack is read from bottom to top. Show abstract face markers only, no denomination, portrait, seal, emblem, milled edge, coin engraving, or monetary symbol. At most 12 tiny ink-green short lines may support direction. The complete app frame includes the exact four-item bottom navigation and only "工具" selected.
4. "完成 · 240ms": The exact title remains "六爻起卦". Show three completed lines stacked from bottom to top in generation order: solid "初爻 · 阳 · 静爻" at the bottom, solid "二爻 · 阳 · 静爻" above it, and broken "三爻 · 阴 · 静爻" above the second line. Keep "和值 8 · 少阴 · 静爻" and the exact marker calculation "正、反、正 = 3 + 2 + 3 = 8" visible, and advance progress to "第 4／6 爻" without changing previous lines. Use 4 px to 0 vertical settle, scale 1.02 to 1, and at most 8 restrained non-celebratory particles. The next action reads "投下一爻". The complete app frame includes "首页", "工具", "历史", "设置" in that order with only "工具" selected.
5. "减少动态 · 80ms": The exact title remains "六爻起卦". Show the exact same three completed lines, labels, marker sequence, calculation, "少阴", "阴静爻" meaning, progress "第 4／6 爻", and next action through opacity only, with no rotation, translation, scale, blur, or particles. A small chip reads "减少动态". Focus and semantic reading order match frame 4. The complete app frame includes the same four-item bottom navigation with only "工具" selected.

Line-order and rule consistency gate: Lines are generated from 初爻 upward to 上爻. In this five-state example the visible stack grows from bottom to top; never place 初爻 above 二爻 or the third generated line below them. A solid unbroken line must be labeled "阳"; a broken line must be labeled "阴". The third result is fixed as "正、反、正", "3 + 2 + 3 = 8", "少阴", "阴静爻", and "静爻"; every graphic, label, sum, and state must agree exactly. In press and generation, the third result slot is one pixel-identical neutral empty placeholder and must not leak any third result through 正／反分色, color, shape, line style, symbol, orientation, brightness, or contrast.

Motion semantics: Use timing annotations 90ms, 360ms, 640ms, 240ms, 80ms. Press easing is cubic-bezier(0.2,0,0,1); generation is cubic-bezier(0.4,0,0.2,1); reveal is cubic-bezier(0.16,1,0.3,1); complete is cubic-bezier(0.2,0,0,1); reduced motion is linear. Make the timeline communicate that interruption or resume preserves completed lines and never tosses the same line again.

Typography and hierarchy: Render only the quoted Chinese and timings. Keep the exact title, progress, generated status, line result, navigation, and next action in a clear hierarchy. The five frames must look like consecutive states of one implementable interface.

Constraints: High fidelity, modern motion expression, refined typesetting, controlled material depth, practical Flutter/Web components, clear hierarchy, accessible labels, opaque background. Build entirely from this revised written specification. Do not use any prior rejected image as an edit input, visual reference, layout source, or asset source.

Avoid and prohibit: no watermark, no brand, no trademark, no real or realistic currency, no denomination, no traditional or copyrighted tarot card artwork, no casino or gambling imagery, no concept art, no low-fidelity wireframe, no cheap gradient, no exaggerated glassmorphism, no transparent background, no antique scroll, no mystical landscape, no real coin, no extra text, no fortune claim, no repeated random action, no third-line preview during press or generation, no solid-line and yin-label mismatch, no broken-line and yang-label mismatch, no reversed line order, no confetti, and no third-party symbol.
