Use case: ui-mockup
Asset type: high-fidelity mobile motion storyboard for tarot
Primary request: Create an implementation-ready five-state Pocketools tarot motion storyboard showing press, generate, reveal, complete, and reduced-motion behavior. The random card identity and orientation are already generated and persisted before reveal. This is product UI motion specification, not fantasy concept art.

Canvas and framing: One exact 3840 x 2160 landscape canvas with an opaque neutral backdrop. Place five complete equal portrait mobile frames in one horizontal sequence, each preserving the 360 x 800 viewport ratio, full frame border, consistent scale, and readable 4K typography. Use a thin timeline baseline and one concise timing chip per frame; do not crop screens or add device branding.

Shared visual language: Pocketools is a restrained, refined modern utility toolbox. Use a deep neutral shell with #111416 background, #181C1F work surfaces, #22282C muted surfaces, #293036 raised surfaces, #F4F6F7 primary text, #BEC6CC secondary text, and #46515A borders. Tarot uses #C7A9EA and #E4BC63 only as controlled accents on a #2A2034 weak surface. Build depth through whitespace, precise typography, matte materials, subtle 1 px edge highlights, and restrained shadows. Use a system sans-serif, an 8-point grid, 16 px standard card radius, 12 px control radius, 48 px minimum touch targets, and a visible 2 px focus ring. Avoid template cards, occult wallpaper, stars, moons, smoke, or decorative mysticism.

Navigation invariant: Every one of the five complete mobile app frames shows the same four-item bottom navigation in the exact order "首页", "工具", "历史", "设置". This is the tarot tool route, so "工具" is selected in every frame and the other three items are visibly unselected. During generation the navigation may be semantically disabled, but it must remain visible and must not become a hamburger menu, top drawer, or device-level navigation.

Five frames, left to right:
1. "按压 · 90ms": The primary button text is exactly "抽取三张牌" and is visibly pressed by translateY 1 px and scale 0.985, with zero blur and no particles. The interface shows "单牌" and "三牌" choices with "三牌" selected, fixed positions "过去", "现在", "未来", and "使用逆位" enabled. The complete app frame includes the four-item bottom navigation with only "工具" selected.
2. "生成 · 360ms": Inputs and primary action are locked, a compact status reads "正在生成结果", the abstract card stack remains face-down, scale settles from 0.985 to 1, blur never exceeds 0.6 px, and there are no particles. Keep "三牌" selected, preserve the same three positions and enabled reverse setting, and do not reveal identity. The complete app frame still includes "首页", "工具", "历史", "设置" in that order with only "工具" selected.
3. "揭示 · 640ms": Status reads "结果已生成，正在揭示". Show three abstract geometric cards assigned to "过去", "现在", and "未来"; one card is halfway through a controlled Y-axis flip while the other two remain stable. Use maximum 12 px upward travel, scale 0.96 to 1, blur 8 to 0 px, and at most 12 tiny abstract purple-gold points or short lines across the entire frame. No traditional scene, person, named card, numeral, suit, esoteric seal, or copyrighted composition. The complete app frame includes the exact four-item bottom navigation and only "工具" selected.
4. "完成 · 240ms": Show all three settled abstract card results in the fixed order "过去", "现在", "未来"; one has visible text "逆位". Each card has exactly one concise keyword row, fixed verbatim as "过去 · 关键词：澄见", "现在 · 关键词：转隙", and "未来 · 关键词：落定" respectively; all three rows must be visible and must not be replaced, omitted, or merged. Use a calm outcome plane, scale 1.02 to 1, 4 px to 0 vertical settle, and at most 8 tiny non-celebratory particles across the frame. Include "查看过程" and "再来一次" only in the completed state. The complete app frame includes "首页", "工具", "历史", "设置" in that order with only "工具" selected.
5. "减少动态 · 80ms": Show the same settled result reached through opacity only, no rotation, translation, scale, blur, or particles. A small state chip reads "减少动态". Layout, focus destination, result content, three-card mode, positions, and enabled reverse setting exactly match frame 4. Show all three keyword rows again, fixed verbatim as "过去 · 关键词：澄见", "现在 · 关键词：转隙", and "未来 · 关键词：落定" respectively. The complete app frame includes the same four-item bottom navigation with only "工具" selected.

Motion semantics: Use timing annotations 90ms, 360ms, 640ms, 240ms, 80ms. Press easing is cubic-bezier(0.2,0,0,1); generation is cubic-bezier(0.4,0,0.2,1); reveal is cubic-bezier(0.16,1,0.3,1); complete is cubic-bezier(0.2,0,0,1); reduced motion is linear. Visually communicate that skip, interruption, or resume lands on the same persisted completed result and never redraws.

Configuration and text consistency gate: All five frames represent one unchanged three-card session. "三牌" remains selected, "使用逆位" remains enabled, and the ordered positions remain "过去", "现在", "未来". Before generation the only primary action wording is exactly "抽取三张牌"; never replace it with "生成结果", "抽牌", or another synonym. The only allowed numeric text is the five timing annotations 90ms, 360ms, 640ms, 240ms, and 80ms. Do not show a deck size, remaining-card count, card number, inventory count, badge count, or any unexplained numeral such as 36.

Typography and hierarchy: Render only the quoted minimal Chinese, the five exact timings, and the three fixed short keyword rows. Keep all text crisp and unmoving. Every repeated word, position, state, result, navigation label, and keyword must agree across the five frames. The five states must read as one continuous product interaction, not five unrelated screens.

Constraints: High fidelity, modern motion expression, refined typesetting, controlled material depth, practical Flutter/Web components, clear hierarchy, accessible state labels, opaque background. Build entirely from this revised written specification. Do not use any prior rejected image as an edit input, visual reference, layout source, or asset source.

Avoid and prohibit: no watermark, no brand, no trademark, no real or realistic currency, no traditional or copyrighted tarot card artwork, no recognizable Rider-Waite-like composition, no casino or gambling imagery, no concept art, no low-fidelity wireframe, no cheap gradient, no exaggerated glassmorphism, no transparent background, no fantasy scenery, no neon, no smoke, no celestial wallpaper, no extra text, no deck count, no remaining-card count, no unexplained number, no re-draw action during reveal, no configuration change between states, no confetti, and no third-party symbol.
