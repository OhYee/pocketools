# 贡献指南

感谢你为 Pocketools 提交问题、文档、设计、代码或测试。Pocketools 当前处于 v0.1.1 源码基线维护期，贡献需要尊重需求追踪、设计门禁、隐私和内容许可边界。

## 开始之前

请先阅读：

- [README.md](README.md)：项目定位、首版范围和当前状态。
- [docs/README.md](docs/README.md)：文档索引和后续交付物。
- [docs/requirements.md](docs/requirements.md)：稳定需求 ID、优先级和验收条件。
- [docs/project-plan.md](docs/project-plan.md)：角色边界、阶段门禁和 Definition of Done。
- [docs/licensing.md](docs/licensing.md)：第三方代码、内容和视觉资产的许可要求。

根目录 Makefile 是统一入口。安装 Flutter stable 后运行 `make bootstrap`，提交前至少运行 `make verify`；Android 产物另需 JDK 17 与 Android SDK 36，并运行 `make build-android`。任何未运行的平台命令必须在变更说明中明确标注。

## 可以贡献什么

- 需求澄清、规则边界和可访问性建议。
- 交互文档、关键页面设计稿和有许可记录的视觉资产。
- 规则实现、平台适配、内容包、测试和缺陷修复。
- 文档、示例、测试夹具和开发体验改进。

新工具或新商业能力不属于默认范围。请先提交范围说明，由项目经理确认是否需要新增 FR、NFR 或 REL 需求。

## 需求与设计门禁

- 每个实现或测试变更都应在说明中引用受影响的 FR、NFR 或 REL ID。
- 关键页面必须先有交互文档和设计稿，并通过项目计划中的 G1 设计就绪门禁，开发才可以开始业务代码。
- 设计变更不得静默改变随机规则、隐私默认值、内容边界或分享字段。
- 发现需求冲突时，保留现有行为并提交影响分析，等待项目经理裁决。
- 不用“顺手重构”扩大变更范围；无关改动应拆开说明。

## 代码与测试要求

实现提交需要说明：

- 改动覆盖的需求 ID。
- 规则版本、算法版本或 schema 版本是否变化。
- Android 与 Web 是否都受影响。
- 新增或修改的测试，以及尚未验证的场景。
- 任何数据迁移、分享、日志、权限或网络行为变化。

规则层应能够注入固定随机序列，界面动画不能直接访问随机源。随机源不可用时不得降级为时间戳或普通伪随机。测试不得把未验证的统计结果写成“公平证明”。

## 内容、资产与许可证

提交牌面、卦义、规则文本、字体、图像、图标、音效或生成式资产时，必须同时提供：

- 来源、作者或版权方。
- 许可证名称、链接和允许的使用范围。
- 是否修改、合成或裁剪。
- 所需归属文本以及应出现的产品入口。
- 是否允许应用内置、商店展示、分享和再分发。

来源不明或许可待确认的资产只能作为设计参考，不能进入发布产物。具体格式见 [docs/licensing.md](docs/licensing.md)。

## 隐私与安全

- 不在问题、备注、随机结果、分享载荷或日志中使用真实个人敏感信息。
- 不新增账号、远程上传、分析字段或设备权限而不更新需求、隐私说明和测试范围。
- 安全漏洞、隐私泄露和依赖漏洞不要在公开问题中披露，按 [SECURITY.md](SECURITY.md) 的方式报告。

## 提交变更的检查清单

在提交评审前确认：

- [ ] 变更范围与需求 ID 一致。
- [ ] 设计、实现和测试没有越过当前阶段门禁。
- [ ] 正常、边界、错误、离线、恢复和无障碍状态已说明。
- [ ] 没有提交密钥、真实用户数据或未审查资产。
- [ ] 第三方内容和依赖的许可信息已同步。
- [ ] 文档只有一个一级标题，标题不编号，不使用独立水平分隔线。
- [ ] 未验证的命令、指标和兼容性结论没有写成事实。

## 评审与合并

评审者会优先检查需求覆盖、规则正确性、隐私默认值、跨端一致性、无障碍、许可和回退路径。P0 需求失败、数据丢失、随机源不安全、默认分享泄露或资产许可缺失时，不应合并到发布候选。

具体代码平台、分支保护和评审人配置尚未在当前仓库基线中确定；相关规则将在发布渠道确认后补充，状态为【待确认】。
